li = list(input())
zero, one = li.count('0')//2, li.count('1')//2
for _ in range(zero):
    li.pop(-li[::-1].index('0')-1)
for _ in range(one):
    li.pop(li.index('1'))
print(''.join(li))

#0은 뒤에서부터 없애고 1은 앞에서부터 없앤다.
# [::-1]는 처음부터 끝까지(::) 뒤에서부터(-) 1칸(1)간격으로 탐색한다는 의미이다.
# [::2]는 처음부터 끝까지(::) 앞에서부터 2칸(2)간격으로 탐색한다는 의미이다.
# [1:6:2]는 인덱스가 1인거부터(1) 6까지(6) 2칸(2)간격으로 탐색한다는 의미이다.
