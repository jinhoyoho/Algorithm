n = int(input())
scary = list(map(int, input().split()))

scary.sort()
group = list()
i = result = 0

while i < n: 
    group.append(scary[i])

    if(max(group) == len(group)): 
        #그룹의 최댓값과 갯수가 같으면 -> 그룹 결성 완료
        result += 1
        group = list() #그룹 초기화
    
    i += 1

print(result)
